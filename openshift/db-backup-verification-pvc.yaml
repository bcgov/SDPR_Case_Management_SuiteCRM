apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-backup-verification-pvc
  labels:
      app: suitecrm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: netapp-file-standard
  resources:
    requests:
      storage: 2Gi