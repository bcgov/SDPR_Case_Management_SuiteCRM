apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backup-storage-cron.fullname" . }}-test-connection"
  labels:
    {{- include "backup-storage-cron.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backup-storage-cron.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
